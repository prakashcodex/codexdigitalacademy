[build]
  publish = "."

# Netlify Identity redirects
[[redirects]]
  from = "/access"
  to = "/course.html"
  status = 200

[[headers]]
  for = "/*"
    [headers.values]
      X-Frame-Options = "SAMEORIGIN"
      X-Content-Type-Options = "nosniff"
